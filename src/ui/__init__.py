# src/ui/__init__.py
"""
Optional User Interface package for the SVG Handwriting Generation project.

Contains modules for graphical interaction (e.g., using Tkinter).
"""

# You might expose the main App class if it's intended to be imported elsewhere,
# but typically GUIs are run as main scripts.
# from .gui import HandwritingApp
# __all__ = ["HandwritingApp"]

# Often, UI packages don't need to expose anything and can be empty if
# the GUI is launched via a specific script or command.